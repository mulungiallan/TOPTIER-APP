Google Play review blockers — STATUS (updated 2026-09-23 #2)

CURRENT REPORT (from Play Console):
  Privacy policy page returns a page not found error
    Evidence URL: https://toptier-production.up.railway.app/privacy

VERIFICATION — the page is NOT 404; the report is STALE/TRANSIENT.
  Live probes against the deployed Railway service all return HTTP 200:
    - GET  /privacy                    -> 200, ~35 KB real policy HTML, <1s
    - GET  /privacy/  (trailing slash) -> 200
    - GET  /privacy?foo=1              -> 200
    - HEAD /privacy                    -> 200
    - GET  /privacy  (Googlebot UA)    -> 200
    - 8 consecutive warm request       -> 200 each
    - /account-deletion                -> 200 (same checks)
  No redirects, no auth wall, no geo-block, valid TLS, static SSR'd content.

  Likely cause: Google's checker ran during the 2026-09-23 redeploy (service
  restarts drop requests) or against a stale report. Nothing to fix in code.

ACTION — force Google to re-check (do these in order):
  1. Deploy is settled (no builds running). Confirm yourself:
     Invoke-RestMethod https://toptier-production.up.railway.app/api/health
     -> {"status":"ok","db":"ok"}
  2. Play Console (https://play.google.com/console) -> TOPTIER app.
  3. Upload the new release android/release/toptier-release.aab (v1.1.1 /
     versionCode 7) on the Release track.
  4. In the release review screen, the Privacy policy + Data safety questions
     must both show green checks (URLs already set to the Railway URLs).
  5. Click "Send app for review" — this re-runs the policy URL check.
     If it still flags, click "I have fixed issues with my privacy policy
     page" to force a re-check after confirming /api/health is "ok".

NOTES:
  - account-deletion URL: https://toptier-production.up.railway.app/account-deletion
  - If you prefer branded URLs later, wire app.toptier.app to Railway first
    (CNAME @ -> d3zueru2.up.railway.app + _railway-verify TXT in toptier.app DNS).